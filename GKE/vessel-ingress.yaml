apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: "gce"
  name: shippy-ingress
spec:
  tls:
  - hosts:
    - www.adzmash.com
    secretName: shippy-secret
  backend:
    serviceName: nginx
    servicePort: 443
